package etiya.SportsClub.repository;

import etiya.SportsClub.entity.CourseBundleRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CourseBundleRecordRepository extends JpaRepository<CourseBundleRecord, Long> {
}
